<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="generic_task_queue.TaskAutoRefreshField">
        <!-- Label: custom text or task display name; shown only while active -->
        <t t-if="isActive and displayLabel">
            <span t-out="displayLabel"/>
        </t>

        <!-- Progress block: spinner + bar while task is active -->
        <t t-if="props.showProgress and _currentTaskId() and isActive">
            <div class="gtq-task-progress d-flex align-items-center gap-2 mt-1">
                <span class="fa fa-spinner fa-spin text-muted"
                      style="cursor: pointer;"
                      title="Click to refresh"
                      t-on-click="onClickReload"/>
                <div class="progress flex-grow-1" style="height: 6px;">
                    <div
                        class="progress-bar"
                        role="progressbar"
                        t-att-style="'width: ' + state.progress + '%'"
                        t-att-aria-valuenow="state.progress"
                        aria-valuemin="0"
                        aria-valuemax="100"
                    />
                </div>
                <small class="text-muted" t-out="state.progress + '%'"/>
            </div>
        </t>
    </t>
</templates>
